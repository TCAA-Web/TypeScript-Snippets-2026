// Create a random number between 1 and x
let randomNumber = Math.floor(Math.random() * 1100) + 1
console.log("Random number: ", randomNumber)

// Round up a decimal number
let decimal = 420.69;
let rounded = Math.ceil(decimal)
console.log("Decimal rounding ", rounded)

// Create a calculation that returns a decimal
let nummer = (4 / 2.2).toFixed(2)
console.log("Calc that returns decimal ", nummer)