# DOM methods

// Get a specific element by ID
// Get a specific element by className
// Update elements textContent property
// Update attributes on element (add data-theme to h1)

# Strings

// Capitalize every first letter in a string
// Remove all whitespaces in a string
// Replace all slashes in a string with spaces

# Numbers

// Create a random number between 1 and x
// Round up a decimal number
// Create a calculation that returns a decimal ( eg. 3 / 2)

# Objects

// Create a new object (car - model - top speed - width - length)
// Add color to car object using spread operator
// Update color in car object to another one
// Create a new instance of car object and give it another color
// Iterate through car object properties and print every line to DOM

# Arrays

// Create an array of names
// Print a specific name from array (index)
// Iterate through array of names printing every line
// Add a new entry to array (push)
// Remove the last entry in an array (pop)
// Edit specific entry in array to something else
// Find specific index and remove it
// Sort array by names descending

# Functions

// Create a void function (updates car object)
// Create a function that returns a new car object
// Create a function that capitalizes first letter in string
// Create and use an async function that fetches on a specific URL and returns the data (or error message)

# Assignment:

Fetch all products from dummyjson - Then update the title of every product with a prefix: "NEW". Now display all the products in the DOM and create a sort button that sorts them alphabetically, both ascending and descending when pressed.
